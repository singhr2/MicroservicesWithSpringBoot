# Unique name for the service (referred in Eureka Server)
#----------------------------------------------
spring.application.name=zuul-proxy


#
#----------------------------------------------


#==============================================
#    Config server related
#==============================================

# server.port from application.properties of config server
spring.cloud.config.uri=http://localhost:8012

#name used in Github repo to store this service's
#Note: Used _ instead of - to avoid confusion in repo file naming
# Create file in Github as  config_server-dev.properties
spring.cloud.config.name=zuul_proxy
spring.profiles.active=dev
